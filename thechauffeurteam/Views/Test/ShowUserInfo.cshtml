@model IEnumerable<thechauffeurteam.Models.job>
@{

}
<br /><br />
<div class="container">
    <h3 style="text-align:center">-----Employee List-----</h3><br />
    <table class="table table-bordered">
        <tr>
            <th style="width:20%">ID</th>
            <th style="width:80%">price</th>

            <th style="width:80%">phone</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr id="tr1">
                @*<td>@item.DriverId</td>*@
                <td>
                    <a class="dropdown-item" href="" onclick="showinfo(@item.id)" data-toggle="modal" data-target="#jobModal">Info</a>
                    
                    
                </td>
                <td class="td1">@item.PassengerName</td>
                <td class="td1">@item.PassengerPhone</td>
            </tr>
        }
    </table>
</div>

<br /><br />
<br /><br />
<script src="~/Scripts/jquery-3.3.1.min.js"></script>
<script src="~/Scripts/popper-utils.min.js"></script>
<script src="js/popper.min.js" type="text/javascript"></script>
<script>



     function jobInfo(id) {
            //alert("success" + Id)


             $.ajax({
                type: "POST",
                 url: '@Url.Action("EmployeeInfo", "Test")',
                 data: { id: Id },
                 dataType: "json",

                 success: function (success) {

                         //alert('pick Up :'+success.pickUp);
                         var txt = $("#jobId").val();
                         $("#jobId").html(txt + success.jb.id);
                         $("#jid").val(success.jb.id);
                         $("#pickUpAddress").val(success.jb.pickUp);
                         $("#dropOffAddress").val(success.jb.DropUP);
                         $("#distance").val(success.jb.Mile);
                         $("#price").val(success.jb.Price);
                         $("#carType").val(success.jb.CarType);

                     

                         $(success.drivers).each(function (index, item) {
                             //if (item.DriverId != null) {
                                 select.append($("<option>").val(item.Id).html(item.DriverId));
                             //}
                             //else {
                             //    select.append($("<option>").val(item.Id).html(item.DriverName));
                             //}
                         });
                         console.log(success);
                 },
                 error: function (e, text, string) {
                    //alert(e.message + text + string);
                    console.log(e);
                 }

            });


    };


    $(document).ready(function () {
        $.each
        $("#tr1").mouseover(function () {
            var data = $(this).attr("value");
           


        })


    });



    //$(document).ready(function () {
    //    $('[data-toggle="popover"]').popover({
    //        title: setData,
    //        html: true,
    //        placement: 'right'

    //    });
        //$(".td1").mouseover(function () {
        //    var id = $(this).text('value')
        //    console.log(id);
        //    alert(id);



       // });
    //    function setData(id) {
    //        var set_data = '';
    //        var element = $(this);
    //        var id = element.attr("id");
    //        $.ajax({
    //            url: "/Test/EmployeeInfo?id" + id,
    //            method: "POST",
    //            async: false,
    //            data: { id: id },
    //            success: function (data) {
    //                set_data = data;
    //            }

    //        });
    //        return set_data;
    //    }
    //});
</script>